import java.util.ArrayList;


public class MadonnaPiangeSborraDalCUlo {
 public static void main(String[] args) {
	 Song a=new Song();
	 a.setName("piscio");
	 ArrayList<Song> b= new ArrayList<Song>();
	 b.add(a);
	 Song c=new Song();
	 c.setName("piscio");
	 if (!b.contains(c) ) System.out.println("a");
	 
	 /*boolean found=false;
		for(int j=0; i<song.size();i++){
			if(song.get(j).getName().equals(toAdd.getName())) {
				found=true;
			}
		}
		if (!found) song.add(toAdd);*/
	 
 }
}
